# Configuración para desarrollo local usando Railway PostgreSQL
# Configura estas variables de entorno en tu sistema o IDE

# URL externa de Railway PostgreSQL (para desarrollo local)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://maglev.proxy.rlwy.net:47286/railway}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:WGrxvBMfELuDRejDcCdujVAOcFgiOfKz}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA/Hibernate para PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuración de logs para desarrollo
logging.level.com.minegocio=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Configuración de servidor para desarrollo
server.port=8080
server.address=localhost 