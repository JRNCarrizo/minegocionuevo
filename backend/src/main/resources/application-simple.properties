# ===== CONFIGURACIÓN SIMPLE - CAMBIAR BASE DE DATOS =====
# 
# INSTRUCCIONES:
# 1. Para usar H2 (desarrollo local): DESCOMENTAR la sección H2 y COMENTAR la sección PostgreSQL
# 2. Para usar PostgreSQL (producción): DESCOMENTAR la sección PostgreSQL y COMENTAR la sección H2
# 3. Guardar el archivo y reiniciar la aplicación

# ===== CONFIGURACIÓN H2 (DESARROLLO LOCAL) =====
# DESCOMENTAR ESTA SECCIÓN PARA USAR H2
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ===== CONFIGURACIÓN POSTGRESQL (PRODUCCIÓN) =====
# DESCOMENTAR ESTA SECCIÓN PARA USAR POSTGRESQL
# spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://maglev.proxy.rlwy.net:47286/railway}
# spring.datasource.driverClassName=org.postgresql.Driver
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:WGrxvBMfELuDRejDcCdujVAOcFgiOfKz}
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=update

# ===== CONFIGURACIÓN COMÚN =====
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.defer-datasource-initialization=true

# Configuración SQL para datos iniciales
spring.sql.init.mode=always
# Para H2 usar data-h2.sql, para PostgreSQL usar data.sql
spring.sql.init.data-locations=classpath:data-h2.sql

# Configuración de logs
logging.level.com.minegocio=INFO
logging.level.org.hibernate.SQL=WARN

# Configuración de seguridad
spring.security.basic.enabled=false

# Configuración de servidor
server.port=${PORT:8080}
server.address=0.0.0.0

# Configuración de Actuator para health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.metrics.enable.processor=false
management.metrics.enable.jvm=false
management.metrics.enable.system=false

# ===== CONFIGURACIÓN JWT =====
jwt.secret=tuClaveSecretaMuyLargaYSeguraParaJWT2024
jwt.expiration=86400000

# ===== CONFIGURACIÓN DE ARCHIVOS =====
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload-dir=./uploads

# ===== CONFIGURACIÓN DE EMAIL =====
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=tu-email@gmail.com
spring.mail.password=tu-password-de-aplicacion
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ===== CONFIGURACIÓN DE CACHE =====
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=500,expireAfterWrite=600s 