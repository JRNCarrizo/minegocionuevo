/** Cambiar a true cuando quieras volver a mostrar registro en el dominio principal. */
export const REGISTRO_PUBLICO_HABILITADO = false;

/** Cambiar a true cuando quieras volver a mostrar login en el catálogo del subdominio. */
export const LOGIN_CATALOGO_HABILITADO = false;
